import { APP_NAME } from "./Brand";


export const ACCESS_TOKEN = `${APP_NAME}-access-token`;
export const ID_TOKEN = `${APP_NAME}-id-token`;
export const REFRESH_TOKEN = `${APP_NAME}-refresh-token`;
export const USER_PROFILE = `${APP_NAME}-user-profile`;
export const USER_LOCATION = `${APP_NAME}-user-location`;
export const USER_CREDENTIALS = `${APP_NAME}-user-credentials`;

// ── Onboarding (U4) — the personality-match funnel persists its choices here.
export const ONBOARDING_TONE = `${APP_NAME}-onboarding-tone`;
export const ONBOARDING_MENTOR = `${APP_NAME}-onboarding-mentor`;
export const ONBOARDING_COMPLETE = `${APP_NAME}-onboarding-complete`;