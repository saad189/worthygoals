/**
 * feed tab (Q4 · Hi-Fi nav).
 *
 * The "feed" — status, reactions, weekly review — is the motivational board,
 * which already exists under `inspiration/`. U2 surfaces it as a top-level tab;
 * the full polyphonic feed reskin lands in its own flow (U6). Re-export keeps a
 * single source of truth for the board screen.
 */
export { default } from "@/app/inspiration/default-screen";
